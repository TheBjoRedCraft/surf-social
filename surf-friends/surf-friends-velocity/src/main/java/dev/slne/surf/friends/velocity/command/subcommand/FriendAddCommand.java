package dev.slne.surf.friends.velocity.command.subcommand;

import dev.jorel.commandapi.CommandAPICommand;

public class FriendAddCommand extends CommandAPICommand {
    public FriendAddCommand(String commandName) {
        super(commandName);
    }
}
