package dev.slne.surf.friends.velocity.command.subcommand;

import dev.jorel.commandapi.CommandAPICommand;

public class FriendRemoveCommand extends CommandAPICommand {
    public FriendRemoveCommand(String commandName) {
        super(commandName);
    }
}
